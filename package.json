{
  "name": "restaurant-agency",
  "private": true,
  "scripts": {
    "dev": "npx concurrently \"node agency-core/index.js\" \"node gateway/index.js\" \"cd frontend && npx vite --port 5173\"",
    "build": "cd frontend && npx vite build",
    "start": "node scripts/start.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@types/node": "^25.9.3",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.18.0",
    "better-sqlite3": "^12.10.0"
  }
}
